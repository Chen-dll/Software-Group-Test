1.虚拟按键执行语句全部存放在自己写的type.h文件内，生成的文件会自动调用。
2.完善了大部分标点符号，另外我暂时用@来代替回车。
3.test.cpp是在测试调用.h文件时放的，用于测试总输出结果。
4.我的电脑打不开最终程序，好像是因为不兼容，只能手动编译，不知道真正效果。
5.编译时只需编译main.cpp就行。有一个问题，编译后的main.exe单独打开会无法调用gcc编译指令，但可以正常输出文件。
6.还是用的char数组，其实问题不大，但是我不知道如何分行输入。还有一个问题，如果文段长了，编译ou.cpp时间会奇长无比（因为没有原文），不知道如何解决。
7.把load.bat,main.cpp和type.h放在一个目录中，它们是一个项目中的。邮箱发不了.bat文件，但和原来是一模一样的，把1.bat改成load.bat就行